{{-- v2 accessibility statement is identical to v1: both render the standalone
Viki-template page with no collection layout, so the skin version is irrelevant. --}}
@include('geddes.pages.accessibility')
